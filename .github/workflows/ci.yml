name: CI

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  pull_request:
    types: [ "opened", "synchronize", "reopened", "ready_for_review" ]
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  build:
    if: github.event.pull_request.draft != true
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: "-D warnings"
    steps:
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Checkout codebase
        uses: actions/checkout@v3

      - name: Format
        run: cargo fmt --check

      - name: Lint
        run: cargo clippy --all-targets

      - name: Build release
        run: cargo build

      - name: Test
        run: cargo test

  no_std:
    if: github.event.pull_request.draft != true
    runs-on: ubuntu-latest
    steps:
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
          target: thumbv7m-none-eabi

      - name: Checkout codebase
        uses: actions/checkout@v3

      - name: Build no_std (no features)
        run: cargo build -p strong-type --no-default-features

      - name: Build no_std + alloc
        run: cargo build -p strong-type --no-default-features --features alloc

      - name: Verify on no_std target
        run: cargo build -p strong-type --target thumbv7m-none-eabi --no-default-features
